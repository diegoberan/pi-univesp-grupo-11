[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && python manage.py migrate && python manage.py collectstatic --noinput"

[deploy]
startCommand = "gunicorn pi_univesp.wsgi --bind 0.0.0.0:$PORT --workers 2"
rootDirectory = "webapp"
